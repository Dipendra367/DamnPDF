export default function Hero() {
  return (
    <section className="hero">
      <h1>Every tool you need to work with PDFs in one place</h1>
      <p>
        Merge, split, compress, and convert PDFs with DamnPDF. Free, simple, and
        built around the tools that already work.
      </p>
    </section>
  )
}
